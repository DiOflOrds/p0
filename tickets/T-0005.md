---
id: T-0005
titel: "Orchestrator-MVP: Tick-Loop"
typ: task
prozess: swe3
rolle: dev
sprint: 1
status: done
prio: hoch
blocked_by: [T-0004,T-0007]
reviewer: pl
erstellt: 2026-08-05
geändert: 2026-08-06
---

`platform/orchestrator/`: Ein Tick = Board lesen (tickets/*.md parsen) → nächste Aufgabe wählen (Prio, blocked_by) → Skript-Route prüfen → sonst via Gateway an Rollen-Agent → Ergebnis als Commit/Branch + Ticket-Update zurückschreiben → Run-Registry-Eintrag (JSONL). Manuell startbar; Scheduler später.

## Ergebnis (2026-08-06)

`platform/orchestrator/tick.py`: Board lesen + validieren (board.py-Import) → Auswahl (open, Blocker done, Rolle aktiv+ki, Prio/ID) → Registry-Auflösung script_tasks → aufgaben_typen → Rollen-Default → Skript-Route (board.py) oder Gateway → Ergebnis-Commit auf `feature/T-xxxx-<slug>` im Ziel-Repo (Ticket-Feld `repo`, Default Projekt) → Ticket-Update (in_review mit Reviewer bzw. open mit Fehlernotiz; bei Guardrail-Abbruch NOTFALL-MELDUNG.md) → BOARD.md-Regenerierung + Commits. CLI: `--repos --projekt --dry-run --ticket`. Unit-Verifikation: `platform/tests/test_orchestrator.py` (13 Tests). Ticket-Frontmatter erweitert um optionale Felder `aufgaben_typ`, `repo`.
