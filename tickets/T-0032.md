---
id: T-0032
titel: "Backend-MVP: Decision-Inbox, Board/Report-Aggregation, E-Mail-Versand (SWR-020)"
typ: task
prozess: swe3
rolle: dev
sprint: 3
status: done
prio: hoch
blocked_by: [T-0031]
repo: platform
geändert: 2026-08-06
reviewer: arch
erstellt: 2026-08-06
---

## Ziel

Backend-MVP gemäß Architektur (T-0031): Decision-Inbox (offene DRs mit Optionen/Deadline/Default lesen, Entscheidung entgegennehmen und ins Decision Log schreiben), Aggregation Board/Reports/KPI aus den Git-Artefakten, E-Mail-Versand der Reports (D004, geraldine.john90@gmail.com). Unit-Tests mit SWR-Docstring-IDs (T-0026). Deployment-Artefakt als Infra-as-Code (Docker Compose) für die Hub-VM (T-0035).

## Kontext und Inputs

SWR-020 (+ ggf. neue SWR aus T-0030), ADRs (T-0031), guardrails.yaml. SWR-Bezug: SWR-020.

## Ergebnis

**Umsetzung (2026-08-06, DEV):** `platform/backend/` — `server.py` (stdlib-HTTP gemäß ADR-001: 4 GET-API-Endpunkte + POST decision + statische Auslieferung, Pfadschutz, Fehlercodes 400/404/500/503), `aggregation.py` (Board/Reports/KPI read-only aus Arbeitskopie), `inbox.py` (DR-Liste; Entscheidung → Decision-Log-Zeile + Ticket-Notiz + BOARD + selektiver Git-Commit „Mensch via Inbox", ADR-003), `mailer.py` (SMTP via env, ausfalltolerant, D004). Infra-as-Code: `platform/infra/Dockerfile` + `docker-compose.yml` (Hub-VM, T-0035). 10 API-Tests mit SWR-Docstring-IDs (`test_backend.py`, inkl. HTTP-Ende-zu-Ende auf ephemerem Port und Neustart-Äquivalenz); Suite 81 grün; Matrix regeneriert (24 SWRs, einzige Lücke SWR-021 = UI-Abnahme, folgt T-0034). **Review (ARCH):** ADR-konform (stdlib, Commit-Schreibpfad, kein Zustand), Komponentenzuschnitt eingehalten — angenommen.
